name: Build

on:
  workflow_call:
    inputs:
      app_name:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Log
        run: echo "app name ${{ inputs.app_name }}"
      - name: Build the Docker image
        run: docker build . --file Dockerfile --tag ${{ inputs.app_name }}:$(date +%s)
